global:
  scrape_interval: 15s

scrape_configs:
  # Metrics that each service pushes via OTLP are re-exposed by the collector here.
  - job_name: otel-collector
    static_configs:
      - targets: ['otel-collector:8889']
